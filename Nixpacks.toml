[phases.setup]
nixPkgs = ['...', 'php82', 'nodejs_20'] # Memaksa Railway pakai PHP 8.2 dan Node 20

[phases.build]
cmds = ['composer install --no-dev', 'npm ci', 'npm run build']

[start]
cmd = 'php artisan serve --host 0.0.0.0 --port $PORT'